# DM-Code-Agent：本地代码智能体

**技术栈：** Python、FastAPI、SQLite、Python AST、MCP、Docker、Pytest

**项目描述：** 面向本地代码仓库的 AI Coding Agent，覆盖模型接入、工具调用、上下文治理、代码理解、运行审计与基准评测，重点解决多轮代码任务中的上下文膨胀、代码定位困难、决策不可追溯和评测不可复现问题。

## 简历可用版本

- **Agent Runtime 架构：** 针对模型接口、结构化输出和工具协议不一致导致执行链路脆弱的问题，设计 ReAct 单步工具调用循环，统一 Planner、工具执行、生命周期事件与重规划链路；优先接入原生 Function Calling，并以 JSON 容错解析作为兼容回退，降低多供应商模型接入的协议差异。
- **长上下文治理：** 针对多轮读取、编辑和测试造成的 Prompt 膨胀，设计分层上下文管理、观察结果截断与预算触发压缩；在 12 个固定任务、8k 上下文预算的 A/B 实验中，6 个任务触发 25 次有效压缩，压缩检查点的加权上下文 token 平均降低 **19.51%**，单次最高降低 **57.34%**，严格通过率保持不变。
- **代码理解、审计与恢复：** 针对代码库定位慢、变更影响范围不清和 Agent 决策不可追溯的问题，构建 Semantic Workspace、决策证据图与 JSONL Trace；通过结构化索引定位代码与分析影响范围，将需求、读取、修改和验证串联为证据链，并通过 Checkpoint 支持异常恢复与运行回放。
- **扩展机制与运行治理：** 针对策略模块与安全护栏混杂在 Agent 内核、难以独立验证的问题，基于生命周期事件实现插件装配机制；将 read-before-edit、原子写入、写前备份、观察截断与 Checkpoint 作为默认护栏，将重规划等影响决策路径的策略能力按需启用，支持独立 A/B 验证。
- **评测与验证闭环：** 针对 Agent 结果依赖主观演示、缺少统一评测口径的问题，构建并维护 30 题代码维护 Benchmark，并接入官方 SWE-bench Verified Docker Harness；在固定 50 题公开集上获得 **28/50（56%）官方 resolved**，项目全量测试 **639 passed, 1 skipped**。

## 面试口径

- 上下文实验只统计实际发生的 25 次成功压缩检查点；12 题整体严格通过率在 `full` 与 `no_compression` 两组中均为 8/12，不将结果表述为准确率提升。
- SWE-bench 为固定 50 题、DeepSeek `deepseek-chat`、当前 Agent 配置下的官方 Harness 结果，反映模型、运行时、提示词、工具和环境的组合表现。
- 量化结果均来自仓库内 Benchmark 报告、Trace 或全量测试输出；面试中按实际模块职责说明实现细节。
