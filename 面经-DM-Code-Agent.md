# DM-Code-Agent 项目面经

## 1. 项目介绍

DM-Code-Agent 是面向本地代码仓库维护的轻量、可插拔、全程可审计 Coding Agent。项目以单一 ReAct 循环完成规划、工具执行和结果回填，并通过生命周期扩展、确定性上下文记忆、语义工作区、决策证据图及两级 Benchmark，让执行过程能够被约束、恢复、解释和复测。

## 2. 简历亮点口径

- **轻量执行内核：** 将规划、单步决策、工具执行和失败恢复收敛到一个 ReAct 主循环，避免多个自治模块争夺状态。
- **可插拔运行时：** 以六个可拦截生命周期阶段承载安全、上下文、完成检查和重规划策略，能力可按实验配置独立启停。
- **确定性上下文记忆：** 将旧消息提炼为本地原子记忆，并以 token 正收益作为提交条件，无收益时完整回滚。
- **代码理解与可解释执行：** 增量维护代码语义与影响关系，同时把需求、读取、修改、验证连接为可重建证据链。
- **评测与审计闭环：** 自建隐藏测试 Benchmark 验证工程任务，SWE-bench 交由官方 Docker Harness 判分，Trace 用于解释成功与失败过程。

## 3. 高频面试题

### 一、Runtime 与扩展机制

#### 1. 请介绍这个项目，以及你认为最核心的设计是什么？

**口述回答：**

我做的是一个面向本地代码仓库维护任务的 Coding Agent，目标不只是让模型生成代码，而是把规划、搜索、读取、修改、测试、失败恢复和审计组织成一条稳定执行链。最核心的设计是内核只保留一个 ReAct 循环：每一轮让模型给出一个动作，运行时执行对应工具，再把结构化结果和观察送回下一轮。规划器只提供初始路线，安全守卫、上下文记忆、证据图和重规划都通过生命周期事件装配。这样既保留了 Agent 的动态决策能力，又避免把所有能力硬编码到主循环，便于独立测试、启停和做 A/B 对比。

**追问：为什么强调“只有一个循环”，这与普通 Agent 有何不同？**

我强调的不是代码里只有一个循环语句，而是只有一个执行状态机拥有最终决策权。Planner 不直接执行工具，Capability 也不能私自启动另一套 Agent 循环，它们只能生成计划、修改请求上下文、检查动作或提供反馈。这样 run id、步骤号、对话历史、工具观察和 metadata 都沿同一条链推进，不会出现规划器认为到了第三步、执行器却在另一套状态里的情况。代价是主循环的事件契约必须稳定，所以我把可变策略放在钩子层，并通过测试约束处理顺序和阻断语义。

#### 2. Planner 与 ReAct 主循环是什么关系？

**口述回答：**

我的 Planner 负责把任务拆成有限的计划项，帮助模型先建立方向；真正执行仍由 ReAct 主循环逐轮完成。每一轮模型会结合原始任务、当前计划状态和历史观察选择一个工具，不是机械地把计划项翻译成工具调用。工具成功后，运行时会更新计划完成状态；出现可识别失败时，可以根据配置生成后续新计划。这里要区分两个限制：计划长度控制一次规划输出的复杂度，而 max steps 控制整个 Agent 最多运行多少轮。即使重规划产生了新计划，也仍消耗同一个循环预算，不会无限派生执行流程。

**追问：为什么不严格按计划顺序执行？**

代码维护过程中，只有执行工具后才会暴露真实信息，例如文件不存在、符号位置变化、测试失败原因与预期不同。如果严格按最初计划执行，计划很快就会过期。我把计划定义为可更新的导航，而不是不可变脚本；每轮模型仍要根据最新观察选择动作。为了防止自由度过大，运行时同时有最大步数、重复失败识别、结构化工具状态和可选重规划预算。这样既能响应新证据，也不会因为一次失败无限重写计划或重复调用同一工具。

#### 3. 工具从模型选择到执行完成，完整链路是什么？

**口述回答：**

工具先以名称、描述和 JSON Schema 注册，支持原生工具调用的客户端会把这些定义随 Agent 阶段请求发送给模型。模型返回工具名和参数后，运行时先确认参数是对象并按 Schema 校验，再触发工具调用前钩子，让守卫改参数或阻断。写操作会先做备份，然后调用具体 runner。工具优先返回结构化结果，其中状态、错误码、退出码、改动文件和验证范围属于机器判定，message 只负责展示。随后观察结果被限制长度，再经过结果后钩子，最后统一写入 Step、会话历史、Trace 和 metadata，供下一轮模型决策。

**追问：为什么已有 ToolResult，还保留文本失败判断？**

这是兼容边界问题。内置工具已经可以主动返回 success、failed、unavailable 等结构化状态，运行时应优先信任它；但扩展工具和旧 runner 可能仍只返回字符串，此时完全删除文本判断会让失败被误当成功。因此我的做法是结构化结果优先，结果缺失时才调用观察判定兜底。对内置结构化工具，判定只识别明确前缀、退出码、语法检查标记等位置，避免文件正文里出现 error 就误判；对未知第三方工具才保守全文扫描。它是迁移策略，不是把自然语言重新当主协议。

#### 4. 六个生命周期钩子怎样实现可插拔？

**口述回答：**

六个阶段分别覆盖运行开始、模型请求之前、工具调用之前、工具结果之后、完成之前和运行结束。能力实现统一 install 接口，在装配时拿到事件总线、按阶段包装的模型客户端、TraceWriter 和只读运行状态获取器，然后注册所需处理器。前置工具阶段可以修改参数或阻断，模型请求阶段可以追加上下文，结果阶段可以加工观察，完成阶段可以否决不可信结论，运行首尾阶段可以初始化状态或请求新一轮尝试。主循环只发事件，不认识具体能力，因此新增证据图或安全策略不需要继续增加 enable_xxx 分支。

**追问：某个钩子异常会不会拖垮整个任务？**

事件总线区分中间件、观察者和策略。普通处理器异常时，系统会恢复该处理器调用前的局部数据，写入 hook error，然后继续后续链路，避免一个审计插件让任务整体崩溃。策略型处理器只允许注册在具有决策语义的阶段，例如工具调用前和完成前；如果策略异常或返回非法结构，系统采用保守策略阻断，因为安全政策失效时继续执行风险更高。这个设计不是无条件吞异常，而是根据能力职责选择 fail-open 或 fail-closed，并保留 Trace 供离线定位。

#### 5. 失败后如何重规划？为什么它默认可以关闭？

**口述回答：**

运行时先把工具状态或观察归一成失败信号，再生成失败签名，判断是否与之前重复。启用自适应重规划后，会结合失败类型、重复次数和剩余预算选择策略，生成新的后续计划，并把原因、策略和信号写入 metadata 与 Trace。它默认可关闭，是因为重规划会真实改变模型决策路径、增加调用和 token，不属于纯基础设施护栏。研究或评测时应把它作为独立变量启用，并记录最大重规划次数；read-before-edit、截断、备份等确定性安全措施则可以默认开启，因为它们主要约束边界，不主动替模型改变任务策略。

**追问：不开 Adaptive Replanning 就完全不会恢复吗？**

不会。关闭的是失败信号到“重新生成计划”的自适应策略，不是 ReAct 循环本身。工具失败的观察仍会回到模型，模型下一轮可以主动换路径、重新读取或修改参数；例如读取不存在文件后改为创建目标文件，不一定需要调用 Planner。Adaptive Replanning 的价值是对重复失败和特定错误进行更显式的策略映射，但它也可能增加额外计划请求并让模型偏离原问题。因此我会用同一任务集比较 resolved、步骤数、重规划次数和失败类别，而不是先验地认定开启一定更好。

### 二、上下文、安全与持久化

#### 6. 上下文压缩何时触发，旧消息如何变成记忆？

**口述回答：**

压缩器先排除 system 消息，只有存在足够多可折叠旧消息，并且距离上次压缩达到设定轮次，或者估算上下文超过 token 预算时才触发。它保留最近若干轮，把更旧的 user 与 assistant 消息交给本地记忆层。记忆层不请求 LLM，而是用确定性规则提取任务、文件、工具、错误、验证结果等原子事实，记录来源、文件、作用域、置信度、工作区版本和访问统计。随后以最近四条消息构造查询，通过词项相关度、文件重合、重要度、置信度与新鲜度综合排序，渲染少量相关 item 回到模型上下文。

**追问：为什么称为 Mem0 风格，而不是直接说用了 Mem0？**

项目借鉴的是“把长对话转成可检索原子记忆”的思想，但没有依赖 Mem0 服务或它的模型抽取流程，所以不能说集成了 Mem0。我的实现是本地、确定性的：数据结构、抽取规则、作用域、失效、新鲜度和检索排序都由项目代码控制。它的优势是没有额外 LLM 调用、结果可复算、测试稳定；不足是规则抽取的语义概括能力有限。因此简历准确说法是“Mem0 风格原子记忆折叠机制”，面试时主动讲清借鉴点与自主实现边界。

#### 7. 如何保证压缩不会产生负收益或破坏状态？

**口述回答：**

压缩不是生成候选后立即覆盖历史。上下文窗口先保存压缩器完整运行态，包括记忆内容、轮次计数、压缩次数和最近有效折叠；再生成候选历史并分别估算压缩前后 token。只有候选确实更短时才提交，并记录 accepted compaction；如果没有正收益，就恢复快照，使候选写入的记忆和计数变化全部撤销。最近一次已验证有效的折叠可以在同一任务后续请求中复用，导出 checkpoint 时也会保存相关状态。这样“试压缩”不会悄悄污染后续检索或提前消耗压缩节奏。

**追问：12.71% 是怎样计算的，能否说总 token 降低 12.71%？**

不能直接说整个实验的 API 总 token 降低 12.71%。报告只累计 full 组中 accepted compaction 事件的 estimated_tokens_before 与 estimated_tokens_after，数值是 182921 和 159676，节省 23245，再用 23245 除以 182921 得到 12.71%。它证明的是“实际触发并被接受的压缩检查点”存在直接正收益。实验有 20 个任务、12 个任务触发、44 次有效折叠，但覆盖门要求至少 15 个任务，因此覆盖条件未达标；面试时应如实说明统计口径和限制。

#### 8. 文件修改有哪些安全边界？

**口述回答：**

我把安全边界放在工具执行链，而不是只写进提示词。首先参数必须通过 Schema 与必填字段检查；编辑前，文件台账要求本轮已经读取目标，若读取后又发生写入，依赖旧行号的编辑会被判为 stale，要求重新读取。内容锚定编辑会在当前文件重新做唯一精确匹配，因此可在明确条件下放行。写入前把原文件备份到系统临时目录，避免污染工作区 diff；工具返回后再根据 has_effect、no_change 和结构化状态更新台账。观察还会被统一截断，避免超长输出挤占模型上下文。

**追问：为什么备份失败不阻断写入？**

备份在这里被定义为尽力而为的恢复安全网，不是强一致事务。工作区可能只读、临时目录可能不可用，若任何备份异常都阻断，Agent 在正常可写文件上也无法继续。真正的写入安全还来自路径约束、编辑前读取、内容锚定、原子写和 Git diff，因此备份失败可以返回 None 并记录不到 backup_count，而不改变工具主语义。若业务场景要求强监管，则可以通过工具前策略把备份失败升级为 fail-closed；当前默认是在可用性与恢复能力之间选择较温和的边界。

#### 9. 会话日志、Trace、Checkpoint、回放和分叉分别是什么？

**口述回答：**

它们解决的是不同问题。append-only JSONL 是统一持久化载体，每条记录带事件、payload、id 和 parent_id，保存计划、消息、工具调用、步骤、压缩、checkpoint 等事实。Trace 分析是在读侧汇总失败阶段、恢复、验证缺口和风险信号；回放可以按日志重建历史或重放工具；Checkpoint 是某一步可继续运行的状态快照，包含历史、步骤、计划、metadata、记忆与 Capability 状态。分叉则把源日志截到指定 entry 并追加 fork 记录，形成新的历史分支；若该位置之前没有 checkpoint，分支只能审计，不能声称可直接续跑。

**追问：为什么用 append-only，而不是不断覆盖一个状态 JSON？**

覆盖状态文件只能告诉我“现在是什么”，很难证明“怎样变成现在”。append-only 能保留每一步事实，便于定位模型什么时候做了错误判断、哪个钩子修改了参数、测试失败后是否恢复，也能用 id 与 parent_id 构造分支关系。代价是日志会增长，且原始模型输入可能包含敏感信息，所以默认只保存足够审计的结构和长度，完整 LLM I/O 需要显式开启，同时对敏感环境字段脱敏。Checkpoint 仍采用原子替换保存最新可恢复状态，两者分别承担审计日志与恢复快照职责。

#### 10. 模型 API 无状态时，Agent 的“记忆”从哪里来？

**口述回答：**

每次 HTTP 模型请求本身不会自动记住上一次调用，Agent 必须重新发送当前 messages。短期状态来自 conversation history，其中包含任务、模型动作和工具观察；长任务时 Context Window 根据预算构造派生消息窗口，并可能用原子记忆替代较旧历史。工具成功还会产生独立 evidence memory，例如读取、写入和验证事实，并带文件版本与作用域。运行恢复时，Checkpoint 重新加载历史和 compressor state。也就是说，记忆属于 Agent 运行时，不属于 DeepSeek 或其他模型服务；Planner 请求只生成结构化计划，不需要执行本地工具，因此不会携带 Agent 工具表。

**追问：为什么工具列表通常每次 Agent 请求都发送？**

因为这些模型请求是独立的，无服务器端会话保证。原生 Function Calling 的工具定义位于每次请求的 tools 字段中，模型必须在当前请求里知道可选工具名称、说明和参数 Schema，才能返回合法 tool call。只在第一次发送而后续省略，除非供应商提供明确的持久会话协议，否则模型并不知道仍可调用哪些本地工具。代价是工具定义也占上下文，所以实际工程中应控制工具数量、按能力激活工具，并让 description 简洁；业务约束仍需由运行时守卫执行，不能只依赖 Schema。

### 三、代码理解、证据与评测

#### 11. 语义工作区解决了什么问题？

**口述回答：**

语义工作区解决的是代码结构事实重复解析和修改后影响关系过期的问题。首次扫描 Python 文件时，它通过 AST 提取类、函数、方法、签名、行号和名称引用，用文件 SHA-256 判断后续是否变化，并持久化到 SQLite。查询符号时优先使用 FTS5/BM25，环境不支持时回退 LIKE。运行开始会更新索引，写工具产生实际改动后只更新对应路径，再分析 changed files、affected files 和 related tests。它不是大模型记忆，而是随工作区版本变化的结构化代码知识层，为工具查询和影响反馈提供事实。

**追问：它会直接决定 Agent 修改哪个文件吗？**

不会把索引结果当成强制决策。启用相应能力后，语义工作区可以向下一次模型请求注入有界的变更影响摘要，相关索引工具也可供 Agent 主动查询，但最终工具选择仍由 ReAct 循环完成。这样可以减少盲目搜索，却不能保证候选路径一定正确。尤其动态导入、运行时注册和跨语言依赖不容易由 Python AST 完整解析。因此我把它定位为辅助代码理解与风险提示，并通过 Trace 记录注入内容和版本，后续应以推荐相关测试的 precision、recall 或消融 resolved 变化验证真实收益。

#### 12. 为什么影响图局部更新还要重建部分边？

**口述回答：**

基础 refs 只是“某个名称在某文件某行出现”的事实，impact edge 还要结合当前模块和符号表解析它指向谁。一个文件修改后，不仅它自己的 import、call、inherit 出边会变化；原来指向它的其他 source，也可能因为目标符号新增、删除或改名而需要重新解析。因此局部更新会确定受影响 source 集合，删除这些 source 发出的旧边，再用最新 refs、files 和 symbols 重建。它不会清空整张图，所以未受影响文件的边应保留；同时测试局部结果与从空库全量重建的 edge set 等价。

**追问：局部与全量一致能证明什么？**

它证明在测试覆盖的变化类型下，增量算法没有为了性能牺牲最终图语义。具体要覆盖调用目标切换时旧边消失新边出现、新增符号后模糊引用变精确、删除符号后失效精确边消失、无关 source 的边不被误删。对同一最终源码状态，一条路径执行局部更新，另一条路径清空影响索引后全量建立，最终比较规范化 edge set。这个指标是正确性不变量，不等于线上效果提升；性能收益还需另测扫描量或耗时，Agent 效果则需任务级消融。

#### 13. 决策证据图如何构建，它会影响执行吗？

**口述回答：**

证据图通过生命周期钩子观察运行过程：开始时建立需求节点，同步 Planner 产生计划节点；成功读取产生 observation，实际写入产生 change，测试、lint 或脚本检查产生 verification，完成请求产生 conclusion。边表达计划由需求驱动、修改基于哪些观察、验证覆盖哪些变更，以及失败验证与结论是否矛盾。图并非纯离线记录：状态变化且存在待发送摘要时，会在 Agent 请求前注入有界进度摘要；若已知失败验证与成功结论矛盾，完成前钩子会阻断一次，要求模型处理或解释。

**追问：为什么只阻断一次，而不是一直不允许完成？**

证据图定位为低干预能力，不是完整形式化验证器。失败测试可能与任务无关、环境不可用，或者模型已经解释无法修复；若每次都永久阻断，Agent 会在步数耗尽前反复尝试完成。第一次阻断用于把矛盾明确反馈给模型，之后仍允许结束，但图状态继续标记 contradicted，Trace 分析会暴露验证缺口。这样既让证据真实影响决策，又避免审计能力接管整个执行。对于高风险生产场景，可以另写更严格的 policy capability 强制验证通过。

#### 14. 自建 Benchmark 如何判分？严格通过与隐藏测试通过为何不同？

**口述回答：**

每道题先在隔离工作区写入初始源码和测试，运行 Agent 前后分别建立文件快照，再执行隐藏测试。隐藏测试通过只说明功能结果满足未暴露断言；严格通过还会检查 Agent 是否正常完成、改动范围是否符合任务契约、必要文件是否存在以及 patch 是否有效，因此可能出现隐藏测试过了但修改了不允许文件而严格失败。Runner 汇总 pass rate、隐藏测试率、步骤、工具调用、token、耗时、失败类别、恢复和 Trace 信息。允许文件范围可显式写入 prompt，但这会改变实验条件，比较时必须固定配置。

**追问：当前简历中的 26/30 有证据吗？**

当前仓库中我能核验的 scoped 报告是 28/30，严格通过率和隐藏测试通过率都为 93.3%；另一个未声明范围的 final 报告是严格 20/30、隐藏 28/30。没有找到严格 26/30 对应的 JSON，因此面试时不能把 26/30 当作已核验结果。正确做法是找到那次运行产物，确认 suite signature、任务列表、模型、温度、最大步数、范围声明和能力开关；若找不到，就把简历改成有报告支撑的数字，并说明不同配置为何不可直接混用。

#### 15. SWE-bench 的预测、判分与结果分析流程是什么？

**口述回答：**

预测阶段先选择固定实例，把官方镜像中的 `/testbed` 仓库物化到宿主工作区，再启动容器作为 shell 和测试后端。Agent 在真实仓库副本中修改源码，结束后通过 Git 提取 patch，写成官方 predictions JSONL；预测阶段不自行判断 resolved。评测阶段在独立虚拟环境中调用官方 SWE-bench Harness，把 patch 应用到对应镜像并执行 F2P 与 P2P 测试，只有目标失败测试修复且原通过测试不回归才算 official resolved。分析器再合并预测、官方报告与 Trace，解释空 patch、步骤耗尽、解析错误、工具失败和回归。

**追问：当前 35/50 的简历数字可靠吗？**

当前仓库里没有找到 35/50 对应的官方报告。可见的 50 题报告 `swebench-verified-crossrepo-50-20260806.json` 是 submitted 50、completed 43、resolved 21、empty patch 7；此前对话里还出现过 27/50 等其他运行，但也不能替代 35/50 的原始产物。因此我会把 35/50 标为待补证据，而不是硬背。要恢复该主张，至少要保存官方 report、对应 predictions、50 题 selection、模型与开关配置，并用 summarize 或 analyze 命令复算 35/50=70%。

## 4. 源码证据索引

| 主题 | 证据文件 | 关键入口 |
| --- | --- | --- |
| ReAct 主循环 | `dm_agent/core/agent.py` | `ReactAgent.run`、`_run_once` |
| 共享运行态 | `dm_agent/core/run_state.py` | `Step`、`RunContext` |
| Planner 与重规划 | `dm_agent/core/planner.py`、`replan.py` | 计划生成、失败签名、预算 |
| 生命周期 | `dm_agent/core/events.py` | `EventBus` 与六类 emit |
| Capability 契约 | `dm_agent/core/capabilities.py` | `CapabilityContext`、`AgentCapability` |
| 工具协议 | `dm_agent/tools/base.py`、`core/tool_invoker.py` | `ToolResult`、`Tool`、`ToolInvoker` |
| 失败识别与截断 | `dm_agent/core/observation.py` | `is_failure_observation`、`ObservationBounder` |
| 编辑安全 | `dm_agent/core/guards.py`、`checkpoint.py` | `ReadBeforeEditGuard`、`backup_file` |
| 上下文压缩 | `dm_agent/memory/context_compressor.py`、`core/context_window.py` | `should_compress`、`plan_compaction`、收益门 |
| 语义工作区 | `dm_agent/workspace/engine.py`、`impact.py` | `update`、`search_symbols`、`analyze_impact` |
| 证据图 | `dm_agent/core/evidence.py`、`extensions/capabilities/evidence.py` | 图模型、钩子装配、完成门 |
| Trace 与分叉 | `dm_agent/tracing/writer.py`、`session.py`、`fork.py` | `record`、条目链、`fork_session` |
| Checkpoint | `dm_agent/core/checkpoint.py` | `RunCheckpoint`、原子保存与恢复 |
| 自建评测 | `dm_agent/benchmarks/runner.py` | 快照、隐藏测试、严格评分、汇总 |
| SWE-bench | `swebench_verified/predict.py`、`evaluate.py`、`analyze.py` | 预测、官方判分、诊断合并 |

## 5. 高风险主张清单

1. **所有权：** 面试时要区分原作者已有能力、自己新增或重构的功能，以及仅做过分析但未实现的设想。
2. **单一循环：** 可解释为单一执行状态机，不能声称整个项目只有一个 Python 循环。
3. **任意步骤分叉：** 任意 entry 可生成分支日志，但只有此前有 checkpoint 才能直接 resume。
4. **压缩 12.71%：** 仅代表 accepted checkpoint 的直接估算降幅；覆盖门未达到 15 个不同任务。
5. **自建指标：** 当前可核验 scoped 结果为 28/30，不支持 26/30。
6. **SWE 指标：** 当前仓库不支持 35/50；补齐官方报告前应视为待确认。
7. **语义工作区效果：** 已证明实现与注入，不等于已证明提升 resolved；需要任务级消融才能声称增益。

## 6. 练习方法

每次只练一道主问题：先在两分钟内口述，再沿追问回答设计边界，最后打开证据索引中的源码核对。评分重点不是术语数量，而是能否说明“问题是什么、数据怎样流动、失败时怎么办、为何这样取舍、用什么证据验证”。
